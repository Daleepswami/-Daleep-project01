
# Create a 1D NumPy array containing numbers from 5 to 50 with a step of 5.

import numpy as np
arr1 = np.arange(5, 51, 5)
arr1
     

# 2. Create a 2D array of shape (4, 3) filled with ones.

matrix = np.ones((3,4))
matrix

     

# 3. Create an array of 6 equally spaced values between 0 and 1.
x = np.linspace(0,1,6)
x
     

matrix = np.eye(3,3)
matrix
     

# 5. Find the shape, size, number of dimensions, and data type of a given array.
print(x.size)
print(x.shape)
print(x.ndim)
print(type(x))
     

# Convert the Python list [10, 20, 30, 40, 50] into a NumPy array.

new = np.array([10, 20, 30, 40, 50])
new
     

# Reshape a 1D array of 16 elements into a 4×4 matrix.
x = np.arange(1,17)
x.reshape(4,4)
     

# Flatten a 2D array into 1D using both flatten() and ravel(). Observe the
flat = x.flatten()
print(flat)
rav = x.ravel()
print(rav)
     

# Access the middle element of a 1D array.
middle_element = flat[len(flat) // 2]
middle_element
np.median(flat)
     

# Reverse a NumPy array without using loops.
print(flat[::-1])

     

# Extract all even-indexed elements from a NumPy array.
print(flat[::2])
     

# 2. Extract the last column from a 2D array.
x = np.arange(1,17).reshape(4,4)
x
     

x[:,2]
     

# Slice a 1D array to get alternate elements starting from index 1
alternate_elements = flat[1::2]
alternate_elements
# .

     

# Slice a 1D array to get alternate elements starting from index 1.
alternate_elements = flat[1::2]
alternate_elements

# 4. Extract a 2×2 sub-matrix from a 3×3 matrix.
matrix_3x3 = np.arange(1, 10).reshape(3, 3)
sub_matrix_2x2 = matrix_3x3[:2, :2]
sub_matrix_2x2

# 5. Print all elements except the first and last element of an array.
middle_elements = flat[1:-1]
middle_elements
     

# Select all elements greater than 25 from a NumPy array.
greater_than_25 = flat[flat > 25]

# Replace all negative values in an array with 0.
flat_no_neg = np.where(flat < 0, 0, flat)

# Extract only the elements divisible by 3.
divisible_by_3 = flat[flat % 3 == 0]

# Count how many elements are greater than the mean of the array.
count_greater_than_mean = np.sum(flat > np.mean(flat))

# Find the indices of elements where the value is equal to 50.
indices_equal_50 = np.where(flat == 50)[0]
     

# Add two NumPy arrays element-wise.
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
sum_arrays = a + b

# 2. Perform matrix multiplication of two compatible matrices.
mat1 = np.array([[1, 2], [3, 4]])
mat2 = np.array([[5, 6], [7, 8]])
mat_mult = np.dot(mat1, mat2)

# 3. Add a scalar value 10 to every element of a NumPy array.
arr = np.array([1, 2, 3, 4])
arr_plus_10 = arr + 10

# 4. Add a 1D array of shape (3,) to a 2D array of shape (2, 3) using broadcasting.
arr_2d = np.array([[1, 2, 3], [4, 5, 6]])
arr_1d = np.array([10, 20, 30])
broadcast_sum = arr_2d + arr_1d
     
